import React from "react";
import './styles.scss';

export class NetflixLabel extends React.Component {
    render(){ 
        return (
            <span className="ntlabel">netflixroulette</span>
        )
    }
}